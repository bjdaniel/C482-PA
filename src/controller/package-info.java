/**
 * The controller package contains classes that support passing user supplied data into the model and updating the
 * user view based on model updates and application logic/rules.
 */
package controller;